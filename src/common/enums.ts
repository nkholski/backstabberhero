export enum EFacing {
  Left = -1,
  None = 0,
  Right = 1
}

export enum EEnemyType {
  Walker,
  Stands,
  Sleeper
}

export enum ETurnState {
  Walk,
  Turn,
  Watch,
  TurnBack
}

export enum ETurnTimes {
  WalkTime = 200,
  TurnTime = 50
}
